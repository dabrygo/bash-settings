# Try changes with `source ~/.bashrc`

# Change command defaults
alias du='du -h -d1'
alias ls='ls -va --color'

# Vim
## Use as default editor
export EDITOR='vim'
export VISUAL='vim'
## Use hotkeys on CLI
set -o vi

